#pragma once

#include <QColor>

/// COLOR ///
/// Output display color constants.
///
namespace Color
{
    const QColor ERR = QColor(255, 0, 0);
    const QColor COMPLETE = QColor(0, 255, 0);
    const QColor STANDARD = QColor(238, 238, 238);
    const QColor BACKGROUND = QColor(16, 16, 16);
}
